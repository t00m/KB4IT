= KB4IT — Turn your AsciiDoc files into a browsable knowledge base
:toc: preamble
:toclevels: 2

image:https://img.shields.io/github/v/release/t00m/KB4IT?include_prereleases&sort=semver[Latest release,link=https://github.com/t00m/KB4IT/releases]
image:https://img.shields.io/pypi/v/KB4IT[PyPI,link=https://pypi.org/project/KB4IT/]
image:https://img.shields.io/pypi/pyversions/KB4IT[Python versions,link=https://pypi.org/project/KB4IT/]
image:https://img.shields.io/badge/license-GPL--3.0--or--later-blue[License,link=LICENSE]
image:https://img.shields.io/badge/platform-GNU%2FLinux-lightgrey[Platform]
image:https://github.com/t00m/KB4IT/actions/workflows/kb4it.yml/badge.svg[CI,link=https://github.com/t00m/KB4IT/actions/workflows/kb4it.yml]

*KB4IT* turns a folder of AsciiDoc (`.adoc`) files into a fast, static,
property-indexed website — no database, no JavaScript framework, no
runtime. Write in plain text, commit to git, generate the site, serve
the static files anywhere.

* *Serverless output* — plain HTML/CSS, host on GitHub Pages, S3, nginx,
or just open `index.html`
* *Smart incremental builds* — only changed documents are recompiled
  (content-hash based)
* *Property-driven navigation* — every document attribute becomes a
  filterable index page
* *Three themes out of the box* — `techdoc`, `book`, `blog` — plus a
  theming system if you want your own

Demos: https://github.com/t00m/kb4it-adocs

== Quick start

[source,bash]
----
# 1. Install
pipx install KB4IT

# 2. Create a new knowledge base
kb4it create techdoc ~/mykb

# 3. Build the website
kb4it build ~/mykb/config/repo.json

# 4. Open it
xdg-open ~/mykb/target/index.html
----

You'll also need the `asciidoctor` command on your system (see
<<requirements>>).

== Why KB4IT?

If you've tried MkDocs, Hugo, Sphinx, or Docusaurus and felt any of:

* "I just want to drop `.adoc` files in a folder"
* "I want my metadata (Author, Category, Status…) to become navigation
  automatically"
* "I want the *output* to be static HTML I can rsync anywhere"
* "I don't want Node, Ruby gems, or a 200-plugin config"

…then KB4IT is built for you. It's opinionated, single-purpose, and
tries to stay out of your way.

== Installation

KB4IT ships through several channels. Pick the one that fits:

[cols="1,3,2",options="header"]
|===
| Channel | Command | Notes

| *pipx* (recommended)
| `pipx install KB4IT`
| Isolated user install from PyPI

| *pip*
| `pip install --user KB4IT`
| Same, but into your user site-packages

| *Docker*
| `docker pull ghcr.io/t00m/kb4it:latest` +
  _(once published)_
| Image includes `asciidoctor`, no host deps needed

| *.deb* (Debian/Ubuntu)
| `sudo dpkg -i kb4it_<version>_all.deb`
| Installs into `/opt/kb4it/venv`, symlinks `/usr/bin/kb4it`

| *.rpm* (Fedora/RHEL)
| `sudo dnf install kb4it-<version>.noarch.rpm`
| Same layout as the .deb

| *From source*
| `git clone https://github.com/t00m/KB4IT && cd KB4IT && pipx install . --force`
| For hacking / unreleased versions

| *Source tarball*
| Download from the https://github.com/t00m/KB4IT/releases[releases page]
| SHA-256 sums published alongside
|===

Release artefacts are built with the scripts under
`scripts/distribution/` — see
`scripts/distribution/README.md` if you want to build them yourself.

[[requirements]]
== Requirements

* GNU/Linux (tested on Debian, Ubuntu, Fedora)
* Python ≥ 3.11
* `asciidoctor` (Ruby gem, install via your package manager)

[source,bash]
----
# Debian / Ubuntu
sudo apt install asciidoctor

# Fedora
sudo dnf install asciidoctor
----

== Usage

[source,bash]
----
kb4it create <theme> <repo_path>   # scaffold a new repo
kb4it build <config.json>          # build the site
kb4it info <config.json>           # show repo stats
kb4it themes                       # list available themes
kb4it apps <theme>                 # list theme apps
kb4it --version                    # show version
----

A KB4IT repository is just three directories and a config file:

[source]
----
mykb/
├── config/
│   └── repo.json       # title, theme, source, target, workers, sort
├── source/             # your .adoc files
└── target/             # generated static website (output)
----

A minimal `repo.json`:

[source,json]
----
{
  "title":   "My Knowledge Base",
  "tagline": "Notes I don't want to lose",
  "theme":   "techdoc",
  "sort":    "Date",
  "force":   false,
  "workers": 4,
  "source":  "/home/me/mykb/source",
  "target":  "/home/me/mykb/target"
}
----

== Themes

[cols="1,3",options="header"]
|===
| Theme | Best for

| *techdoc*
| Technical documentation, runbooks, knowledge bases. Dense, searchable,
  property-heavy navigation.

| *book*
| Long-form structured content. Chapter/section layout.

| *blog*
| Chronological posts with tags/categories.
|===

Custom themes live in `~/.kb4it/opt/resources/themes/` or inside the
installed package.

== How it works (30-second tour)

[source]
----
Your .adoc files
     │
     ▼
  Processor  ──── extracts metadata (properties), detects changes by hash
     │
     ▼
  Compiler   ──── runs asciidoctor in parallel workers → HTML fragments
     │
     ▼
  Builder    ──── renders Mako templates for the chosen theme
     │
     ▼
  Deployer   ──── copies static site to your target directory
----

Service-oriented architecture. See `CLAUDE.md` for a full module map.

== Contributing

Contributions are very welcome — especially:

* Bug reports with a minimal reproducing repo
* New themes (drop a folder under `kb4it/resources/themes/` or publish
  your own)
* Documentation improvements (this is the #1 place KB4IT can get better)
* Performance work on the compiler / builder

How to start:

[source,bash]
----
git clone https://github.com/t00m/KB4IT
cd KB4IT
pipx install . --force        # or: pip install -e . --user
kb4it --version
----

Open an issue before starting a large change so we can align on the
approach. A PR with a clear problem statement and a minimal diff beats
a big "improvements" PR every time.

== Roadmap / known limitations

* Documentation is thinner than the code deserves — help wanted
* No online editor by design — use your preferred editor
* No dynamic search — KB4IT aims to stay small and serverless
* API is still evolving; minor releases may change internals
* When a linked document's properties change, related property pages may
  need a `--force` rebuild to refresh titles

== Credits

* http://www.python.org/[Python] — the language KB4IT is written in
* https://asciidoctor.org[Asciidoctor] — text processor and publishing
  toolchain
* https://getuikit.com[UIKit] — the front-end framework used by the
  default themes
* https://www.geany.org/[Geany] — the editor used to build KB4IT

== License

KB4IT is released under the link:LICENSE[GNU GPL v3 or later].

== Contact

Tomás Vírseda (aka *t00m*) — tomasvirseda@gmail.com

If KB4IT is useful to you, star the repo — it genuinely helps me decide
where to spend time. Issues, ideas, and PRs are all welcome.
